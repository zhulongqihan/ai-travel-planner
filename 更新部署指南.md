# 🚀 更新 GitHub 和 Docker 镜像指南

本文档提供完整的代码提交和Docker镜像更新流程。

---

## 📋 更新前检查清单

- [ ] 所有代码已测试通过
- [ ] 版本号已更新（如需要）
- [ ] README.md 已更新
- [ ] 截图文件已添加到 `界面功能截图/` 目录

---

## 📤 Step 1: 提交代码到 GitHub

### Windows PowerShell 命令

```powershell
# 1. 查看当前状态
git status

# 2. 添加所有更改的文件
git add .

# 3. 提交更改（请修改提交信息）
git commit -m "feat: 优化图片显示系统，使用CSS渐变背景设计

- 移除对外部图片API的依赖，改用纯CSS渐变背景
- 为每个城市/景点分配独特的渐变色和Emoji图标
- 添加浮动动画效果，提升视觉体验
- 更新README，添加13张功能截图展示
- 优化地图信息窗口UI设计"

# 4. 推送到 GitHub
git push origin main
```

### Linux/Mac 命令（相同）

```bash
# 1. 查看当前状态
git status

# 2. 添加所有更改的文件
git add .

# 3. 提交更改
git commit -m "feat: 优化图片显示系统，使用CSS渐变背景设计

- 移除对外部图片API的依赖，改用纯CSS渐变背景
- 为每个城市/景点分配独特的渐变色和Emoji图标
- 添加浮动动画效果，提升视觉体验
- 更新README，添加13张功能截图展示
- 优化地图信息窗口UI设计"

# 4. 推送到 GitHub
git push origin main
```

---

## 🐳 Step 2: 自动触发 Docker 镜像构建

### 方式一：GitHub Actions 自动构建（推荐）

推送代码到 `main` 分支后，GitHub Actions 会**自动**执行以下操作：

1. ✅ 检测到代码推送
2. ✅ 自动构建 Docker 镜像
3. ✅ 推送到阿里云容器镜像仓库
4. ✅ 标记为 `latest` 和 `main` 标签

**查看构建状态：**
```
https://github.com/zhulongqihan/ai-travel-planner/actions
```

**预计时间：** 5-8 分钟

---

### 方式二：手动触发构建（可选）

如果需要立即构建，可以创建版本标签：

```bash
# 创建版本标签
git tag -a v2.1 -m "版本 2.1: 优化图片显示和UI设计"

# 推送标签到 GitHub
git push origin v2.1
```

---

## ✅ Step 3: 验证更新

### 3.1 验证 GitHub 更新

1. 访问项目页面：https://github.com/zhulongqihan/ai-travel-planner
2. 检查 README.md 是否显示截图
3. 确认最新 commit 已显示

### 3.2 验证 Docker 镜像构建

1. 访问 GitHub Actions：
   ```
   https://github.com/zhulongqihan/ai-travel-planner/actions
   ```

2. 查看最新的 workflow 运行状态：
   - 🟢 绿色对勾 = 构建成功
   - 🔴 红色叉号 = 构建失败（查看日志）
   - 🟡 黄色圆圈 = 正在构建中

3. 构建成功后，访问阿里云镜像仓库确认：
   ```
   https://cr.console.aliyun.com/
   ```

---

## 🔄 Step 4: 测试新镜像

### 本地测试

```bash
# 1. 停止并删除旧容器
docker stop ai-travel-planner
docker rm ai-travel-planner

# 2. 删除旧镜像（可选）
docker rmi crpi-6mppr7h5m3b03dk6.cn-hangzhou.personal.cr.aliyuncs.com/aitravel/ai-travel-planner:latest

# 3. 拉取最新镜像
docker pull crpi-6mppr7h5m3b03dk6.cn-hangzhou.personal.cr.aliyuncs.com/aitravel/ai-travel-planner:latest

# 4. 运行新容器
docker-compose up -d

# 5. 查看日志
docker logs -f ai-travel-planner

# 6. 访问应用测试
# 浏览器打开 http://localhost:8000
```

---

## 📝 完整的更新命令（一键复制）

### Windows PowerShell

```powershell
# ========== 更新 GitHub ==========
git add .
git commit -m "feat: 优化图片显示系统

- 使用CSS渐变背景替代外部图片
- 添加功能截图到README
- 优化UI设计"
git push origin main

# ========== 等待 GitHub Actions 构建完成（5-8分钟）==========
# 访问查看进度：https://github.com/zhulongqihan/ai-travel-planner/actions

# ========== 测试新镜像 ==========
docker stop ai-travel-planner
docker rm ai-travel-planner
docker pull crpi-6mppr7h5m3b03dk6.cn-hangzhou.personal.cr.aliyuncs.com/aitravel/ai-travel-planner:latest
docker-compose up -d
docker logs -f ai-travel-planner
```

### Linux/Mac

```bash
# ========== 更新 GitHub ==========
git add .
git commit -m "feat: 优化图片显示系统

- 使用CSS渐变背景替代外部图片
- 添加功能截图到README
- 优化UI设计"
git push origin main

# ========== 等待 GitHub Actions 构建完成（5-8分钟）==========
# 访问查看进度：https://github.com/zhulongqihan/ai-travel-planner/actions

# ========== 测试新镜像 ==========
docker stop ai-travel-planner && \
docker rm ai-travel-planner && \
docker pull crpi-6mppr7h5m3b03dk6.cn-hangzhou.personal.cr.aliyuncs.com/aitravel/ai-travel-planner:latest && \
docker-compose up -d && \
docker logs -f ai-travel-planner
```

---

## 🐛 常见问题

### Q1: git push 失败，提示 "rejected"

**原因**：远程仓库有新的提交

**解决**：
```bash
git pull --rebase origin main
git push origin main
```

### Q2: Docker 镜像构建失败

**解决**：
1. 查看 GitHub Actions 日志
2. 检查 Dockerfile 语法
3. 确认阿里云镜像仓库访问权限
4. 验证 GitHub Secrets 配置正确

### Q3: 推送速度很慢或超时

**解决**：
```bash
# 增加 Git 缓冲区大小
git config --global http.postBuffer 524288000

# 增加超时时间
git config --global http.lowSpeedLimit 0
git config --global http.lowSpeedTime 999999

# 或者使用 SSH 协议
git remote set-url origin git@github.com:zhulongqihan/ai-travel-planner.git
```

### Q4: 图片在 GitHub 上不显示

**原因**：图片路径错误或文件未提交

**解决**：
```bash
# 确保图片文件已添加
git add 界面功能截图/

# 检查文件是否已暂存
git status

# 提交并推送
git commit -m "docs: 添加功能截图"
git push origin main
```

---

## 📊 更新进度追踪

### 标准流程时间线

| 步骤 | 操作 | 预计时间 |
|------|------|---------|
| 1️⃣ | 代码提交 | 1-2 分钟 |
| 2️⃣ | 推送到 GitHub | 2-5 分钟 |
| 3️⃣ | GitHub Actions 构建 | 5-8 分钟 |
| 4️⃣ | 推送到阿里云 | 自动完成 |
| 5️⃣ | 拉取新镜像测试 | 2-3 分钟 |
| **总计** | **端到端更新** | **10-18 分钟** |

---

## 🎯 快速参考

### 只更新 GitHub（不构建镜像）

```bash
git add .
git commit -m "docs: 更新文档"
git push origin main
```

### 强制重新构建镜像

```bash
git tag -a v2.1 -m "重新构建镜像"
git push origin v2.1
```

### 查看构建日志

```
https://github.com/zhulongqihan/ai-travel-planner/actions
```

### 查看镜像版本

```bash
docker images | grep ai-travel-planner
```

---

## ✅ 更新完成检查清单

- [ ] GitHub 仓库显示最新代码
- [ ] README.md 截图正常显示
- [ ] GitHub Actions 构建成功（绿色✅）
- [ ] 阿里云镜像仓库有新的 `latest` 标签
- [ ] 本地拉取新镜像成功
- [ ] 新容器启动正常
- [ ] 应用功能测试通过

---

<div align="center">

**🎉 恭喜！更新完成！**

如有问题，请参考 [GitHub Issues](https://github.com/zhulongqihan/ai-travel-planner/issues)

</div>

